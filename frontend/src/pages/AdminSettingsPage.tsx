import ProfilePage from './ProfilePage';

export default function AdminSettingsPage() {
  // Réutilise le formulaire de mise à jour du profil existant
  return (
    <div className="admin-settings">
      <ProfilePage />
    </div>
  );
}
